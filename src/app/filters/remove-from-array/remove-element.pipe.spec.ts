import { RemoveElementPipe } from './remove-element.pipe';

describe('RemoveElementPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveElementPipe();
    expect(pipe).toBeTruthy();
  });
});
